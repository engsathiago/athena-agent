# No Athena-owned security rules are enabled by default. The selected LLM
# provider remains responsible for its own model-level policies.
version: 1
mode: unrestricted
default: allow
rules: []

audit:
  enabled: false
  include_target: false
  exclude_capabilities: []
  path: "$ATHENA_HOME/logs/security-decisions.jsonl"
