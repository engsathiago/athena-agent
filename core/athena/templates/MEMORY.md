# Athena durable memory

Entries added by Athena will be stored here when the built-in memory tool is
used. Deeper searchable memory lives in `athena_memory.db` and mirrors approved
built-in memory writes.
